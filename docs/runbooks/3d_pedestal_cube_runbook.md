# Runbook: 3D Pedestal Cube — Spawn & Verification

Purpose
-------
Short runbook to re-run the Designer-approved spawn of the unit cube at world origin and verify the spawn via engine state or screenshot.

Designer Approval
-----------------
- Scene spec: docs/scene_specs/scene_spec.md
- Lead Designer Agent Approval: APPROVED
- Primary cube color (hex): #4CAF50

Spawn performed (this run)
--------------------------
- Spawn parameters used (converted to RGB for engine API):
  - position: [0, 0, 0]
  - color (hex): "#4CAF50"
  - color (RGB): { r: 76, g: 175, b: 80 }

- Exact spawn call used (engine Daemon spawn_cube):
  spawn_cube({ position: [0,0,0], color: { r: 76, g: 175, b: 80 } })

- Spawn response (recorded):
  {
    "success": true,
    "entityId": "cube_001",
    "position": [0,0,0],
    "color": { "r": 76, "g": 175, "b": 80, "a": 255 }
  }

- Verification via get_game_state (recorded output):
  {
    "gameState": {
      "gameMode": "GAME",
      "playerPosition": [-5,0,1],
      "propCount": 5,
      "spawnedCubeCount": 1
    },
    "success": true
  }

Notes: the spawn response included the returned entityId "cube_001" and confirmed position [0,0,0]. get_game_state confirms spawnedCubeCount=1 indicating the engine registered the spawned primitive.

Re-run steps
------------
1) Ensure you have Designer approval and are working in the project workspace.
2) From an environment that can call the engine Daemon, run the spawn call. You may pass color as a hex string if supported, or as an RGB object as shown below.

Example (preferred, using hex if supported):

spawn_cube({
  position: [0, 0, 0],
  color: "#4CAF50"
})

Example (explicit RGB object — produced from #4CAF50):

spawn_cube({
  position: [0, 0, 0],
  color: { r: 76, g: 175, b: 80 }
})

Expected response format:
- success: true
- entityId: "cube_XXX" (example: "cube_001")
- position: [0,0,0]

3) Verify engine state after spawn:
- Call get_game_state() (or engine-specific state query). Confirm at minimum that:
  - spawnedCubeCount increments (>=1)
  - propCount increases appropriately
  - If the engine state contains a listing of entities, locate the returned entityId and confirm its transform.position === [0,0,0]

4) Capture a verification screenshot (optional):
- Use the engine's screenshot API or tooling. Example Daemon call (replace with available tool):

capture_screenshot({ filename: "pedestal_cube_spawn", format: "png" })

- Save the screenshot to a repo-local path or artifact storage and include it in PR or release notes.

5) If the entityId does not follow the expected format or the position is incorrect:
- Re-check input parameters (position, color)
- If engine expects different origin/anchor conventions (e.g., base vs center), adjust transforms accordingly (see scene spec notes about pedestal Y offset)
- Remove the incorrect entity if needed and re-run spawn (engine-specific remove API)

Quick checklist
---------------
- [ ] spawn_cube called with position [0,0,0]
- [ ] color matches Designer primary color #4CAF50 (RGB: 76,175,80)
- [ ] spawn response returns entityId with format cube_XXX
- [ ] get_game_state confirms spawn (spawnedCubeCount or entity listing)
- [ ] (optional) screenshot captured and attached to verification

Recorded outputs (copyable)
---------------------------
Spawn command used:
spawn_cube({ position: [0,0,0], color: { r: 76, g: 175, b: 80 } })

Spawn response (example):
{"success":true,"entityId":"cube_001","position":[0,0,0],"color":{"r":76,"g":175,"b":80,"a":255}}

get_game_state() (example):
{"gameState":{"gameMode":"GAME","playerPosition":[-5,0,1],"propCount":5,"spawnedCubeCount":1},"success":true}

Contact / Notes
----------------
If you need to adjust material parameters (roughness, metalness) or add the pedestal and lights, see docs/scene_specs/scene_spec.md for the full spec and recommended parameters.

Runbook version: 1.0
Generated by: Programmer Agent (task abab12fc-1ac9-4e68-8a2d-df2d97eca728)
Date: 2026-04-01
